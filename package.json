{
  "name": "hiking-fence",
  "version": "0.0.0",
  "scripts": {
    "lint": "npm run eslint",
    "eslint": "eslint src/**/*.js",
    "start": "parcel serve --https src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "gh-pages": "npm run build && gh-pages -d dist"
  },
  "browserslist": [
    "last 1 Chrome version"
  ],
  "dependencies": {
    "@material/mwc-fab": "^0.14.1",
    "@turf/buffer": "^5.1.5",
    "ol": "6.3.2-dev.1587681282362"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "gh-pages": "^3.0.0",
    "parcel-bundler": "^1.12.4"
  }
}
